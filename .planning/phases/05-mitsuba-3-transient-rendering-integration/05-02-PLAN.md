---
phase: 05-mitsuba-3-transient-rendering-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agentsim/physics/mitsuba_detection.py
  - src/agentsim/environment/discovery.py
  - tests/unit/test_mitsuba_detection.py
autonomous: true
requirements: [MIT-05]

must_haves:
  truths:
    - "has_mitsuba_transient() returns True when both mitsuba and mitransient are in discovered packages"
    - "has_mitsuba_transient() returns False when mitsuba or mitransient is missing"
    - "format_mitsuba_scene_context() returns Mitsuba template instructions when has_mitsuba is True"
    - "format_mitsuba_scene_context() returns numpy fallback guidance when has_mitsuba is False"
    - "mitransient is added to KNOWN_SIMULATION_PACKAGES in discovery.py"
  artifacts:
    - path: "src/agentsim/physics/mitsuba_detection.py"
      provides: "Mitsuba availability check and scene context formatting"
      exports: ["has_mitsuba_transient", "format_mitsuba_scene_context"]
    - path: "src/agentsim/environment/discovery.py"
      provides: "Updated with mitransient in KNOWN_SIMULATION_PACKAGES"
      contains: "mitransient"
    - path: "tests/unit/test_mitsuba_detection.py"
      provides: "Unit tests for detection and context formatting"
  key_links:
    - from: "src/agentsim/physics/mitsuba_detection.py"
      to: "src/agentsim/state/models.py"
      via: "EnvironmentInfo import"
      pattern: "from agentsim.state.models import EnvironmentInfo"
    - from: "src/agentsim/physics/mitsuba_detection.py"
      to: "src/agentsim/environment/discovery.py"
      via: "uses EnvironmentInfo produced by discover_environment"
      pattern: "EnvironmentInfo"
---

<objective>
Create the Mitsuba availability detection module and update environment discovery to include mitransient. The detection module checks whether mitsuba+mitransient are available and generates appropriate scene agent context (Mitsuba template instructions vs numpy fallback).

Purpose: Environment-based auto-detection (D-11, D-12) routes the scene agent between Mitsuba template rendering and numpy approximate simulation without CLI flags.
Output: New mitsuba_detection.py module + updated discovery.py + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mitsuba-3-transient-rendering-integration/05-CONTEXT.md
@.planning/phases/05-mitsuba-3-transient-rendering-integration/05-RESEARCH.md

@src/agentsim/environment/discovery.py
@src/agentsim/state/models.py

<interfaces>
<!-- Key types the executor needs -->

From src/agentsim/state/models.py:
```python
class AvailablePackage(BaseModel, frozen=True):
    name: str
    version: str = ""
    import_name: str = ""

class EnvironmentInfo(BaseModel, frozen=True):
    packages: tuple[AvailablePackage, ...] = ()
    python_version: str = ""
```

From src/agentsim/environment/discovery.py:
```python
KNOWN_SIMULATION_PACKAGES: dict[str, str] = {
    "mitsuba": "mitsuba",
    # ... other packages
}

def discover_environment(extra_packages: dict[str, str] | None = None) -> EnvironmentInfo:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mitsuba detection module and discovery.py update</name>
  <files>
    src/agentsim/physics/mitsuba_detection.py,
    src/agentsim/environment/discovery.py,
    tests/unit/test_mitsuba_detection.py
  </files>
  <read_first>
    src/agentsim/environment/discovery.py,
    src/agentsim/state/models.py,
    src/agentsim/preview/renderer.py,
    .planning/phases/05-mitsuba-3-transient-rendering-integration/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test: has_mitsuba_transient returns True when EnvironmentInfo.packages contains both "mitsuba" and "mitransient" names
    - Test: has_mitsuba_transient returns False when only "mitsuba" present (mitransient missing)
    - Test: has_mitsuba_transient returns False when neither present
    - Test: has_mitsuba_transient returns False when only "mitransient" present (mitsuba missing)
    - Test: format_mitsuba_scene_context(has_mitsuba=True) contains "Mitsuba 3" and "mitransient" and "llvm_ad_rgb" and template import example
    - Test: format_mitsuba_scene_context(has_mitsuba=False) contains "Numpy" and "approximate"
    - Test: format_mitsuba_scene_context(has_mitsuba=True) contains "mi.set_variant" before "import mitransient" (correct order per Pitfall 2)
    - Test: format_mitsuba_scene_context(has_mitsuba=True) contains "np.save" (D-09 raw .npy output)
    - Test: "mitransient" is in KNOWN_SIMULATION_PACKAGES in discovery.py
  </behavior>
  <action>
    **1. Update discovery.py** — Add mitransient to KNOWN_SIMULATION_PACKAGES:
    Add this entry to the dict (alphabetical position, after "mitsuba"):
    ```python
    "mitransient": "mitransient",
    ```

    **2. Create src/agentsim/physics/mitsuba_detection.py**:
    - `from __future__ import annotations`
    - Module docstring: "Mitsuba 3 + mitransient availability detection and scene agent context generation."
    - Import: structlog, EnvironmentInfo from agentsim.state.models
    - `logger = structlog.get_logger()`

    Function `has_mitsuba_transient(env: EnvironmentInfo) -> bool`:
    - `names = {p.name for p in env.packages}`
    - `available = "mitsuba" in names and "mitransient" in names`
    - `logger.debug("mitsuba_transient_check", available=available, packages=sorted(names))`
    - `return available`

    Function `format_mitsuba_scene_context(has_mitsuba: bool) -> str`:
    - When `has_mitsuba is False`:
      Return multiline string:
      ```
      \n## Rendering Mode: Numpy Approximate\n
      Mitsuba is not available in the current environment. Use numpy-based
      approximate transient simulation. Mark all outputs as 'approximate'
      in output metadata.\n
      Generate self-contained numpy scripts that simulate transient light
      transport using analytical models (inverse-square falloff, geometric
      path lengths). Save transient data as .npy files.\n
      ```
    - When `has_mitsuba is True`:
      Return multiline string containing:
      - Header: "## Rendering Mode: Mitsuba 3 + mitransient (Physically-Based)"
      - Explanation that mitransient provides NLOS-specific plugins
      - CRITICAL import order: `import mitsuba as mi` then `mi.set_variant('llvm_ad_rgb')` then `import mitransient as mitr` (per Pitfall 2)
      - Template import: `from agentsim.physics.domains.nlos_transient_imaging.templates import (ConfocalPointScene, NonConfocalMeshScene, get_template)`
      - Usage example showing: create template instance with params, call build(), mi.load_dict(), mi.render(), np.save transient as .npy
      - Note about spp=256 default for fast iteration (D-07)
      - Note about saving raw transient as .npy (D-09)
      - Warning: do NOT call mi.set_variant() inside template code
    - Log: `logger.info("mitsuba_scene_context_formatted", mode="mitsuba" if has_mitsuba else "numpy")`

    **3. Create tests/unit/test_mitsuba_detection.py**:
    - Create mock EnvironmentInfo objects with various package combos
    - Test has_mitsuba_transient with 4 cases (both, mitsuba-only, mitransient-only, neither)
    - Test format_mitsuba_scene_context True path contains key strings
    - Test format_mitsuba_scene_context False path contains key strings
    - Test import order in True path: assert "mi.set_variant" appears before "import mitransient"
    - Test discovery.py KNOWN_SIMULATION_PACKAGES contains "mitransient"
  </action>
  <verify>
    <automated>cd /Users/default/mas-664/sim-agents && python -m pytest tests/unit/test_mitsuba_detection.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - discovery.py contains `"mitransient": "mitransient"`
    - mitsuba_detection.py contains `def has_mitsuba_transient(env: EnvironmentInfo) -> bool`
    - mitsuba_detection.py contains `def format_mitsuba_scene_context(has_mitsuba: bool) -> str`
    - mitsuba_detection.py contains `"mitsuba" in names and "mitransient" in names`
    - mitsuba_detection.py contains `mi.set_variant('llvm_ad_rgb')` in the True path output
    - mitsuba_detection.py contains `import mitransient` AFTER set_variant in the True path output
    - mitsuba_detection.py contains `np.save` in the True path output
    - mitsuba_detection.py contains `approximate` in the False path output
    - mitsuba_detection.py contains `logger = structlog.get_logger()`
    - mitsuba_detection.py starts with `from __future__ import annotations`
    - test_mitsuba_detection.py exits 0
  </acceptance_criteria>
  <done>
    has_mitsuba_transient correctly detects mitsuba+mitransient availability. format_mitsuba_scene_context generates appropriate context for both Mitsuba and numpy paths. mitransient added to known packages. All tests pass.
  </done>
</task>

</tasks>

<verification>
- has_mitsuba_transient returns correct boolean for all package combinations
- Mitsuba context includes correct import order (variant before mitransient import)
- Numpy fallback context marks outputs as approximate
- mitransient in KNOWN_SIMULATION_PACKAGES
</verification>

<success_criteria>
- mitsuba_detection.py exists with both exported functions
- discovery.py updated with mitransient entry
- All unit tests pass
- Context formatting follows import order from Pitfall 2 in research
</success_criteria>

<output>
After completion, create `.planning/phases/05-mitsuba-3-transient-rendering-integration/05-02-SUMMARY.md`
</output>
