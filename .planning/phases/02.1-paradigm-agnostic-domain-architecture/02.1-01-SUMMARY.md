---
phase: 02.1-paradigm-agnostic-domain-architecture
plan: 01
subsystem: physics
tags: [pydantic, yaml, nlos, paradigm, sensor-profiles, transfer-functions, frozen-models]

# Dependency graph
requires:
  - phase: 02-computational-imaging-domain-intelligence
    provides: "DomainKnowledge schema, nlos.yaml v1.0, nlos_geometry.py check functions"
provides:
  - "ParadigmKnowledge, SensorProfile, SensorCatalog, TransferFunction, ValidationRule Pydantic models"
  - "relay_wall.yaml and penumbra.yaml paradigm definitions"
  - "sensors.yaml with SwissSPAD2, LinoSPAD2, streak camera profiles"
  - "nlos.yaml v2.0 (shared domain knowledge without paradigm-specific data)"
affects: [02.1-02, 02.1-03, 02.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Paradigm YAML files with validation_rules and transfer_functions"
    - "Sensor profiles as paradigm-independent hardware catalogs"
    - "dict[str, dict[str, float | str]] for flexible geometry constraints"

key-files:
  created:
    - src/agentsim/physics/domains/paradigms/__init__.py
    - src/agentsim/physics/domains/paradigms/relay_wall.yaml
    - src/agentsim/physics/domains/paradigms/penumbra.yaml
    - src/agentsim/physics/domains/sensors.yaml
    - tests/unit/test_paradigm_schema.py
  modified:
    - src/agentsim/physics/domains/schema.py
    - src/agentsim/physics/domains/nlos.yaml
    - tests/unit/test_domain_schema.py

key-decisions:
  - "Used dict[str, dict[str, float | str]] for geometry_constraints to support arbitrary paradigm parameters without schema changes"
  - "Kept original ReconstructionAlgorithm model unchanged; added ReconstructionAlgorithmV2 for D-15 structured knowledge"
  - "Transfer function formulas stored as strings (data model only); evaluation deferred to Phase 02.2"

patterns-established:
  - "Paradigm YAML structure: paradigm, domain, keywords, compatible_sensor_types, geometry_constraints, validation_rules, transfer_functions, published_baselines"
  - "Sensor YAML structure: sensors dict with timing, spatial, noise, operational_modes per profile"
  - "ValidationRule: python_check for complex geometry, range_check/threshold_check for declarative YAML-only rules"

requirements-completed: [PA-04, PA-06]

# Metrics
duration: 5min
completed: 2026-04-07
---

# Phase 02.1 Plan 01: Schema and Data Files Summary

**10 new frozen Pydantic models for paradigm-agnostic architecture with 2 paradigm YAMLs (relay_wall, penumbra), 3 sensor profiles, and refactored nlos.yaml v2.0**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-07T19:22:36Z
- **Completed:** 2026-04-07T19:28:31Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments
- Extended schema.py with 10 new frozen Pydantic models (TransferFunction, ValidationRule, ParadigmKnowledge, TimingParameters, SpatialParameters, NoiseModel, OperationalMode, SensorProfile, SensorCatalog, ReconstructionAlgorithmV2)
- Created relay_wall.yaml (5 validation rules, 2 transfer functions, 3 published baselines) and penumbra.yaml (3 validation rules, 2 transfer functions, 2 published baselines)
- Created sensors.yaml with SwissSPAD2, LinoSPAD2, and streak camera profiles (timing, spatial, noise, operational modes)
- Refactored nlos.yaml to v2.0: removed geometry_constraints and sensor_parameters (moved to paradigm/sensor files)
- 44 total tests passing (34 paradigm schema + 10 domain schema)

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend schema.py with paradigm and sensor models** - `c28b191` (feat) — TDD: RED/GREEN
2. **Task 2: Create YAML data files** - `647b91d` (feat)

## Files Created/Modified
- `src/agentsim/physics/domains/schema.py` - Added 10 new frozen Pydantic models for paradigm-agnostic architecture
- `src/agentsim/physics/domains/nlos.yaml` - Refactored to v2.0: shared domain knowledge only
- `src/agentsim/physics/domains/sensors.yaml` - 3 named sensor profiles with full characterisation
- `src/agentsim/physics/domains/paradigms/__init__.py` - Package marker
- `src/agentsim/physics/domains/paradigms/relay_wall.yaml` - Relay-wall paradigm definition
- `src/agentsim/physics/domains/paradigms/penumbra.yaml` - Penumbra/Plato's cave paradigm definition
- `tests/unit/test_paradigm_schema.py` - 34 tests for new models and YAML validation
- `tests/unit/test_domain_schema.py` - Updated for v2.0 changes

## Decisions Made
- Used `dict[str, dict[str, float | str]]` for geometry_constraints to support arbitrary paradigm parameters without schema changes per paradigm
- Kept original `ReconstructionAlgorithm` model unchanged for backward compatibility; added `ReconstructionAlgorithmV2` with structured knowledge fields
- Transfer function formulas stored as data strings only (no evaluation in Phase 02.1 per D-16)
- Penumbra validation rules are YAML-only (range_check) since no complex geometry logic exists yet (per D-04)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated test_domain_schema.py for v2.0 nlos.yaml changes**
- **Found during:** Task 2 (YAML data files)
- **Issue:** Existing test_domain_schema.py asserted v1.0, geometry_constraints, and sensor_parameters which were removed from nlos.yaml
- **Fix:** Updated 3 tests: version assertion to "2.0", sensor_parameters test to assert None, geometry_constraints test to assert None
- **Files modified:** tests/unit/test_domain_schema.py
- **Verification:** All 10 domain schema tests pass
- **Committed in:** 647b91d (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Expected change from nlos.yaml v1.0 to v2.0. No scope creep.

## Issues Encountered
- Python PYTHONPATH needed to be set to worktree's src directory since the package was pip-installed from the main repo, not the worktree. Resolved by setting PYTHONPATH for test runs.

## User Setup Required
None - no external service configuration required.

## Known Stubs
None - all models are fully defined and all YAML files contain real curated data from published papers.

## Next Phase Readiness
- Schema models ready for Plan 02 (loader/detector refactoring) to use `ParadigmKnowledge.model_validate()` and `SensorCatalog.model_validate()`
- Paradigm YAML files ready for Plan 03 (checker dispatch) to load validation_rules and dispatch checks
- Sensor profiles ready for Plan 04 (agent context injection) to format sensor data into prompts

## Self-Check: PASSED

All 8 created/modified files verified present. Both task commits (c28b191, 647b91d) verified in git log.

---
*Phase: 02.1-paradigm-agnostic-domain-architecture*
*Completed: 2026-04-07*
