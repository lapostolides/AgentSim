---
phase: 02.1-paradigm-agnostic-domain-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agentsim/physics/domains/schema.py
  - src/agentsim/physics/domains/nlos.yaml
  - src/agentsim/physics/domains/sensors.yaml
  - src/agentsim/physics/domains/paradigms/__init__.py
  - src/agentsim/physics/domains/paradigms/relay_wall.yaml
  - src/agentsim/physics/domains/paradigms/penumbra.yaml
  - tests/unit/test_paradigm_schema.py
autonomous: true
requirements:
  - PA-04
  - PA-06

must_haves:
  truths:
    - "ParadigmKnowledge and SensorProfile Pydantic models validate YAML data correctly"
    - "relay_wall.yaml contains geometry constraints, validation rules, and transfer functions"
    - "penumbra.yaml defines a distinct paradigm with its own geometry and validation rules"
    - "sensors.yaml contains at least 3 named sensor profiles (SwissSPAD2, LinoSPAD2, streak camera)"
    - "nlos.yaml retains shared domain knowledge but geometry_constraints are moved to paradigm files"
  artifacts:
    - path: "src/agentsim/physics/domains/schema.py"
      provides: "ParadigmKnowledge, SensorProfile, TransferFunction, ValidationRule, SensorCatalog models"
      contains: "class ParadigmKnowledge"
    - path: "src/agentsim/physics/domains/paradigms/relay_wall.yaml"
      provides: "Relay-wall paradigm definition"
      contains: "paradigm: relay_wall"
    - path: "src/agentsim/physics/domains/paradigms/penumbra.yaml"
      provides: "Penumbra paradigm definition"
      contains: "paradigm: penumbra"
    - path: "src/agentsim/physics/domains/sensors.yaml"
      provides: "Named sensor profiles"
      contains: "swissspad2"
    - path: "tests/unit/test_paradigm_schema.py"
      provides: "Schema validation tests"
  key_links:
    - from: "paradigms/relay_wall.yaml"
      to: "schema.py ParadigmKnowledge"
      via: "Pydantic model_validate"
      pattern: "ParadigmKnowledge.model_validate"
    - from: "sensors.yaml"
      to: "schema.py SensorCatalog"
      via: "Pydantic model_validate"
      pattern: "SensorCatalog.model_validate"
---

<objective>
Define the Pydantic data models and YAML data files for the paradigm-agnostic architecture.

Purpose: Establishes the type contracts and curated data that all downstream plans (loader, formatters, checker, integration) depend on. Per D-01, this is a hierarchical architecture: domain YAML (shared) + paradigm YAML (specific) + sensor YAML (hardware).

Output: Extended schema.py with new models, 2 paradigm YAMLs, 1 sensor YAML, refactored nlos.yaml, and schema validation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-paradigm-agnostic-domain-architecture/02.1-CONTEXT.md
@.planning/phases/02.1-paradigm-agnostic-domain-architecture/02.1-RESEARCH.md
@src/agentsim/physics/domains/schema.py
@src/agentsim/physics/domains/nlos.yaml

<interfaces>
<!-- Existing models in schema.py that must remain backward-compatible -->
From src/agentsim/physics/domains/schema.py:
```python
class ParameterRange(BaseModel, frozen=True):
    min: float
    max: float
    typical: float | None = None
    description: str = ""

class GeometryConstraint(BaseModel, frozen=True):
    three_bounce_path: GeometryConstraintRule | None = None
    relay_wall: dict[str, float | str] = Field(default_factory=dict)
    sensor_to_wall_distance: dict[str, float] = Field(default_factory=dict)
    wall_to_hidden_distance: dict[str, float] = Field(default_factory=dict)

class DomainKnowledge(BaseModel, frozen=True):
    domain: str
    version: str = "1.0"
    description: str = ""
    governing_equations: tuple[GoverningEquation, ...] = ()
    geometry_constraints: GeometryConstraint | None = None
    sensor_parameters: SensorParameters | None = None
    reconstruction_algorithms: dict[str, ReconstructionAlgorithm] = Field(default_factory=dict)
    published_parameter_index: dict[str, PublishedParameterSet] = Field(default_factory=dict)
    dimensionless_groups: tuple[DimensionlessGroup, ...] = ()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend schema.py with paradigm and sensor models</name>
  <files>src/agentsim/physics/domains/schema.py, tests/unit/test_paradigm_schema.py</files>
  <read_first>
    - src/agentsim/physics/domains/schema.py (current models to extend, NOT replace)
    - src/agentsim/physics/domains/nlos.yaml (understand current structure being refactored)
    - tests/unit/test_domain_schema.py (existing tests that must continue passing)
  </read_first>
  <behavior>
    - Test 1: TransferFunction model validates with input="temporal_resolution_ps", output="spatial_resolution_m", relationship="linear", formula="c * dt / 2"
    - Test 2: ValidationRule model validates with type="python_check", module="agentsim.physics.checks.nlos_geometry", function="check_three_bounce_geometry"
    - Test 3: ValidationRule model validates with type="range_check", parameter="relay_wall_size", min=0.1, max=10.0, severity="error"
    - Test 4: ParadigmKnowledge model validates a complete relay-wall paradigm dict with paradigm="relay_wall", domain="nlos_transient_imaging", keywords, compatible_sensor_types, geometry_constraints (dict), validation_rules (tuple), transfer_functions (tuple)
    - Test 5: TimingParameters, SpatialParameters, NoiseModel, OperationalMode, SensorProfile models validate SwissSPAD2 data
    - Test 6: SensorCatalog model validates a dict with 3 sensor entries
    - Test 7: Existing DomainKnowledge model still validates nlos.yaml data (backward compat)
    - Test 8: All new models are frozen (immutable)
  </behavior>
  <action>
    Add the following NEW models to schema.py (keep ALL existing models unchanged, per D-03):

    1. `TransferFunction(BaseModel, frozen=True)`:
       - Fields: input (str), output (str), relationship (str), formula (str = ""), description (str = ""), coupling_strength (str = "")
       - Per D-13/D-14: relationship must be one of: "linear", "inverse", "sqrt", "quadratic", "logarithmic", "proportional", "inverse_sqrt"

    2. `ValidationRule(BaseModel, frozen=True)`:
       - Fields: name (str), type (str), description (str = "")
       - For python_check: module (str = ""), function (str = "")
       - For range_check: parameter (str = ""), min (float | None = None), max (float | None = None), severity (str = "error"), message (str = "")
       - Per D-04: type must be "python_check", "range_check", or "threshold_check"

    3. `ParadigmKnowledge(BaseModel, frozen=True)`:
       - Fields: paradigm (str), domain (str), version (str = "1.0"), description (str = ""), keywords (tuple[str, ...] = ()), compatible_sensor_types (tuple[str, ...] = ()), geometry_constraints (dict[str, dict[str, float | str]] = Field(default_factory=dict)), validation_rules (tuple[ValidationRule, ...] = ()), transfer_functions (tuple[TransferFunction, ...] = ()), published_baselines (dict[str, dict] = Field(default_factory=dict))
       - Per D-01: Paradigms inherit from domain conceptually, but are separate Pydantic models

    4. `TimingParameters(BaseModel, frozen=True)`:
       - Fields: temporal_resolution_ps (ParameterRange | None = None), jitter_fwhm_ps (ParameterRange | None = None), dead_time_ns (ParameterRange | None = None), gate_width_ns (ParameterRange | None = None)

    5. `SpatialParameters(BaseModel, frozen=True)`:
       - Fields: array_size (tuple[int, int]), pixel_pitch_um (float), fill_factor (float = 1.0), fov_degrees (ParameterRange | None = None)

    6. `NoiseModel(BaseModel, frozen=True)`:
       - Fields: dark_count_rate_hz (float = 0.0), afterpulsing_probability (float = 0.0), crosstalk_probability (float = 0.0), quantum_efficiency (float = 0.0)

    7. `OperationalMode(BaseModel, frozen=True)`:
       - Fields: name (str), timing_constraints (dict[str, float] = Field(default_factory=dict)), description (str = "")

    8. `SensorProfile(BaseModel, frozen=True)`:
       - Fields: name (str), sensor_type (str), manufacturer (str = ""), reference (str = ""), timing (TimingParameters), spatial (SpatialParameters), noise (NoiseModel | None = None), operational_modes (tuple[OperationalMode, ...] = ()), transfer_functions (tuple[TransferFunction, ...] = ())

    9. `SensorCatalog(BaseModel, frozen=True)`:
       - Fields: sensors (dict[str, SensorProfile] = Field(default_factory=dict))

    Also add `ReconstructionAlgorithmV2(BaseModel, frozen=True)` extending the existing ReconstructionAlgorithm with:
       - input_requirements (tuple[dict[str, bool | int | str], ...] = ())
       - output_characteristics (tuple[str, ...] = ())
       - transfer_functions (tuple[TransferFunction, ...] = ())
    This is for D-15 structured algorithm knowledge. Keep the original ReconstructionAlgorithm unchanged for backward compat.

    CRITICAL: Do NOT modify or remove ANY existing models. Only ADD new ones.
  </action>
  <verify>
    <automated>cd /Users/default/mas-664/sim-agents && python -m pytest tests/unit/test_paradigm_schema.py tests/unit/test_domain_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - schema.py contains `class TransferFunction(BaseModel, frozen=True):`
    - schema.py contains `class ValidationRule(BaseModel, frozen=True):`
    - schema.py contains `class ParadigmKnowledge(BaseModel, frozen=True):`
    - schema.py contains `class SensorProfile(BaseModel, frozen=True):`
    - schema.py contains `class SensorCatalog(BaseModel, frozen=True):`
    - schema.py contains `class TimingParameters(BaseModel, frozen=True):`
    - schema.py contains `class SpatialParameters(BaseModel, frozen=True):`
    - schema.py contains `class NoiseModel(BaseModel, frozen=True):`
    - schema.py contains `class OperationalMode(BaseModel, frozen=True):`
    - test_paradigm_schema.py exits 0
    - test_domain_schema.py exits 0 (no regressions)
    - All existing models (DomainKnowledge, GeometryConstraint, etc.) are unchanged
  </acceptance_criteria>
  <done>All new Pydantic models defined, frozen, and validated by tests. Existing models unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create YAML data files — paradigms, sensors, and refactored nlos.yaml</name>
  <files>
    src/agentsim/physics/domains/nlos.yaml,
    src/agentsim/physics/domains/sensors.yaml,
    src/agentsim/physics/domains/paradigms/__init__.py,
    src/agentsim/physics/domains/paradigms/relay_wall.yaml,
    src/agentsim/physics/domains/paradigms/penumbra.yaml,
    tests/unit/test_paradigm_schema.py
  </files>
  <read_first>
    - src/agentsim/physics/domains/nlos.yaml (current content to split)
    - src/agentsim/physics/domains/schema.py (models from Task 1 to validate against)
    - src/agentsim/physics/checks/nlos_geometry.py (function signatures for validation_rules references)
    - .planning/phases/02.1-paradigm-agnostic-domain-architecture/02.1-RESEARCH.md (YAML examples)
  </read_first>
  <action>
    **1. Refactor nlos.yaml (per D-03):**
    Remove `geometry_constraints` and `sensor_parameters` sections. Keep: domain, version, description, governing_equations, reconstruction_algorithms (upgrade to include transfer_functions per D-15), published_parameter_index, dimensionless_groups. Bump version to "2.0".

    The reconstruction_algorithms section should add transfer_functions and structured input_requirements/output_characteristics per D-15. Example for lct:
    ```yaml
    lct:
      name: "Light Cone Transform"
      reference: "O'Toole et al. 2018"
      requires_confocal: true
      spatial_resolution: "c/(2*bandwidth)"
      frequency_constraint: "LCT kernel bandwidth sets max recoverable spatial frequency"
      input_requirements:
        - confocal_scanning: true
        - min_scan_points: 16
      output_characteristics:
        - "Spatial resolution limited by relay wall sampling and temporal resolution"
      transfer_functions:
        - input: temporal_resolution
          output: depth_resolution
          relationship: linear
          formula: "c * dt / 2"
          coupling_strength: strong
    ```

    **2. Create paradigms/__init__.py:**
    Empty file (just a docstring: `"""Paradigm YAML files for domain-specific knowledge."""`).

    **3. Create paradigms/relay_wall.yaml (per D-01, D-05):**
    Use the exact YAML structure from RESEARCH.md Pattern 1. Must include:
    - paradigm: relay_wall
    - domain: nlos_transient_imaging
    - keywords: ["relay wall", "three-bounce", "confocal", "three bounce", "relay-wall"]
    - compatible_sensor_types: ["spad", "streak_camera"]
    - geometry_constraints dict with relay_wall, sensor_to_wall_distance, wall_to_hidden_distance (moved from nlos.yaml)
    - validation_rules with 3 python_check entries referencing agentsim.physics.checks.nlos_geometry functions (check_three_bounce_geometry, check_sensor_fov, check_temporal_resolution) and at least 1 range_check entry for wall_size_range
    - transfer_functions with at least 2 entries (temporal_resolution -> spatial_resolution, scan_points -> spatial_nyquist)
    - published_baselines referencing the relay-wall-specific published experiments from nlos.yaml

    **4. Create paradigms/penumbra.yaml (per PA-06):**
    Define the penumbra/Plato's cave NLOS paradigm:
    - paradigm: penumbra
    - domain: nlos_transient_imaging
    - description: "Penumbra imaging (Plato's cave) — reconstructs hidden scene from partial occlusion shadows on a visible surface"
    - keywords: ["penumbra", "plato", "shadow", "partial occlusion", "edge diffraction", "occluder edge"]
    - compatible_sensor_types: ["spad", "gated_iccd", "streak_camera"]
    - geometry_constraints: aperture (min_width_m: 0.001, max_width_m: 0.5, typical_width_m: 0.05), occluder_to_wall_distance (min_m: 0.01, max_m: 10.0, typical_m: 0.5), scene_to_occluder_distance (min_m: 0.01, max_m: 50.0, typical_m: 1.0)
    - validation_rules: range_check for aperture_width (0.001-0.5), range_check for occluder_distance (0.01-10.0) — no python_check functions yet (simple paradigm, per D-04 YAML-only where possible)
    - transfer_functions: aperture_width -> angular_resolution (relationship: inverse), temporal_resolution -> depth_resolution (relationship: linear, formula: "c * dt / 2")
    - published_baselines with at least 1 entry (Bouman et al. 2017 or Saunders et al. 2019)

    **5. Create sensors.yaml (per D-09, D-10, D-11):**
    Use the exact structure from RESEARCH.md sensors YAML example. Must contain 3 sensors: swissspad2, linospad2, streak_camera. Each with timing, spatial, noise, operational_modes, transfer_functions sections. Use the specific values from the research document.

    **6. Add YAML validation tests to test_paradigm_schema.py:**
    Add tests that load each YAML file and validate against the Pydantic models:
    - test_relay_wall_yaml_validates: load relay_wall.yaml, validate as ParadigmKnowledge
    - test_penumbra_yaml_validates: load penumbra.yaml, validate as ParadigmKnowledge
    - test_sensors_yaml_validates: load sensors.yaml, validate as SensorCatalog
    - test_nlos_yaml_v2_validates: load refactored nlos.yaml, validate as DomainKnowledge (backward compat)
    - test_relay_wall_has_validation_rules: assert relay_wall paradigm has >= 4 validation_rules
    - test_sensors_has_three_profiles: assert SensorCatalog has >= 3 sensors
  </action>
  <verify>
    <automated>cd /Users/default/mas-664/sim-agents && python -m pytest tests/unit/test_paradigm_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/agentsim/physics/domains/nlos.yaml does NOT contain "geometry_constraints:" section
    - src/agentsim/physics/domains/nlos.yaml contains "version: \"2.0\""
    - src/agentsim/physics/domains/nlos.yaml contains "governing_equations:"
    - src/agentsim/physics/domains/paradigms/relay_wall.yaml contains "paradigm: relay_wall"
    - src/agentsim/physics/domains/paradigms/relay_wall.yaml contains "check_three_bounce_geometry"
    - src/agentsim/physics/domains/paradigms/relay_wall.yaml contains "transfer_functions:"
    - src/agentsim/physics/domains/paradigms/penumbra.yaml contains "paradigm: penumbra"
    - src/agentsim/physics/domains/sensors.yaml contains "swissspad2:"
    - src/agentsim/physics/domains/sensors.yaml contains "linospad2:"
    - src/agentsim/physics/domains/sensors.yaml contains "streak_camera:"
    - test_paradigm_schema.py exits 0
  </acceptance_criteria>
  <done>All YAML data files created and validated against Pydantic models. nlos.yaml refactored to remove paradigm-specific data. 2 paradigms and 3 sensor profiles defined.</done>
</task>

</tasks>

<verification>
- All new Pydantic models are frozen (immutable)
- relay_wall.yaml, penumbra.yaml, sensors.yaml parse and validate
- nlos.yaml still loads as DomainKnowledge (backward compat)
- Existing test_domain_schema.py still passes
</verification>

<success_criteria>
- 9+ new Pydantic models in schema.py (TransferFunction, ValidationRule, ParadigmKnowledge, TimingParameters, SpatialParameters, NoiseModel, OperationalMode, SensorProfile, SensorCatalog)
- 2 paradigm YAML files with distinct geometry constraints and validation rules
- 1 sensor YAML with 3+ named profiles including all 4 parameter categories (timing, spatial, noise, operational_modes)
- nlos.yaml v2.0 retains shared knowledge without geometry_constraints
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-paradigm-agnostic-domain-architecture/02.1-01-SUMMARY.md`
</output>
