---
phase: 02.1-paradigm-agnostic-domain-architecture
plan: 03
subsystem: physics
tags: [context-formatting, prompt-injection, paradigm-agnostic, yaml]

requires:
  - phase: 02.1-01
    provides: "Schema models (DomainKnowledge, ParadigmKnowledge, SensorCatalog) and YAML data files"
provides:
  - "4 generic context formatters: format_physics_context, format_hypothesis_context, format_analysis_context, format_scene_context"
  - "Paradigm-agnostic prompt injection for all agent roles"
affects: [02.1-04, hypothesis-agent, analyst-agent, scene-agent, physics-advisor]

tech-stack:
  added: []
  patterns: ["generic model iteration for prompt rendering", "role-specific wrapper pattern over base formatter"]

key-files:
  created:
    - src/agentsim/physics/context.py
    - tests/unit/test_physics_context.py
  modified: []

key-decisions:
  - "Base formatter pattern: format_physics_context is the shared core, role-specific formatters wrap it with framing text"
  - "No sensor data in hypothesis context -- hypothesis agent does not need hardware details"
  - "Scene context includes explicit hard-constraint summary with min/max/typical values"

patterns-established:
  - "Generic model iteration: formatters iterate over dict keys and tuple entries, never check paradigm identity"
  - "Role-specific wrapper: each agent formatter adds framing text then delegates to format_physics_context"

requirements-completed: [PA-02]

duration: 3min
completed: 2026-04-07
---

# Phase 02.1 Plan 03: Generic Context Formatters Summary

**4 paradigm-agnostic context formatters rendering any domain/paradigm/sensor YAML data into agent prompt text with zero paradigm-specific code paths**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-07T19:32:12Z
- **Completed:** 2026-04-07T19:35:06Z
- **Tasks:** 1 (TDD: RED -> GREEN)
- **Files modified:** 2

## Accomplishments
- Created `format_physics_context` base formatter that renders domain equations, dimensionless groups, reconstruction algorithms, paradigm geometry/transfer functions, and sensor catalog
- Created `format_hypothesis_context` with hypothesis-specific framing (no sensor data)
- Created `format_analysis_context` with signal physics expectations, reconstruction quality checks, and common failure modes
- Created `format_scene_context` with full D-06 compliance: geometry constraints, sensors, baselines, reconstruction, transfer functions, and hard-constraint summary
- All formatters work identically for relay_wall and penumbra paradigms with zero paradigm-specific branches

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): Failing tests** - `5b78b95` (test)
2. **Task 1 (GREEN): Generic context formatters** - `6b41d1a` (feat)

## Files Created/Modified
- `src/agentsim/physics/context.py` - 4 generic context formatters and 5 private helper functions
- `tests/unit/test_physics_context.py` - 12 tests covering all formatters with relay_wall and penumbra paradigms

## Decisions Made
- Base formatter pattern: `format_physics_context` is the shared core that all role-specific formatters wrap
- No sensor catalog passed to hypothesis context -- hypothesis agent focuses on physics, not hardware
- Scene context includes an explicit "Hard Constraint Summary" section extracting min/max/typical from geometry_constraints for quick reference

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- STATE.md had merge conflicts from parallel agent execution; resolved by keeping the most-recent version

## User Setup Required
None - no external service configuration required.

## Known Stubs
None - all formatters are fully wired to real YAML data.

## Next Phase Readiness
- Context formatters ready for Plan 04 (agent prompt integration) to wire into hypothesis.py, analyst.py, and scene.py
- Existing format_nlos_* functions can be replaced by the new generic formatters

## Self-Check: PASSED

---
*Phase: 02.1-paradigm-agnostic-domain-architecture*
*Completed: 2026-04-07*
