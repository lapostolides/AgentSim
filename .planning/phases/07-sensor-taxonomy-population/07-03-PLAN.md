---
phase: 07-sensor-taxonomy-population
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/agentsim/knowledge_graph/sensors/lidar_mechanical.yaml
  - src/agentsim/knowledge_graph/sensors/lidar_solid_state.yaml
  - src/agentsim/knowledge_graph/sensors/lidar_fmcw.yaml
  - src/agentsim/knowledge_graph/sensors/structured_light.yaml
  - src/agentsim/knowledge_graph/sensors/polarimetric.yaml
  - src/agentsim/knowledge_graph/sensors/spectral.yaml
  - tests/unit/test_sensor_yamls_batch2.py
autonomous: true
requirements: [SENS-06, SENS-09, SENS-10, SENS-11]

must_haves:
  truths:
    - "LiDAR mechanical YAML loads into validated SensorNode objects"
    - "LiDAR solid-state YAML loads into validated SensorNode objects"
    - "LiDAR FMCW YAML loads into validated SensorNode objects"
    - "Structured light YAML loads into validated SensorNode objects"
    - "Polarimetric YAML loads into validated SensorNode objects"
    - "Spectral/hyperspectral YAML loads into validated SensorNode objects"
    - "Each family YAML has family-level ranges and 2-3 concrete sensors"
  artifacts:
    - path: "src/agentsim/knowledge_graph/sensors/lidar_mechanical.yaml"
      provides: "Mechanical LiDAR family"
      contains: "family: lidar_mechanical"
    - path: "src/agentsim/knowledge_graph/sensors/lidar_solid_state.yaml"
      provides: "Solid-state LiDAR family"
      contains: "family: lidar_solid_state"
    - path: "src/agentsim/knowledge_graph/sensors/lidar_fmcw.yaml"
      provides: "FMCW LiDAR family"
      contains: "family: lidar_fmcw"
    - path: "src/agentsim/knowledge_graph/sensors/structured_light.yaml"
      provides: "Structured light family"
      contains: "family: structured_light"
    - path: "src/agentsim/knowledge_graph/sensors/polarimetric.yaml"
      provides: "Polarimetric camera family"
      contains: "family: polarimetric"
    - path: "src/agentsim/knowledge_graph/sensors/spectral.yaml"
      provides: "Spectral/hyperspectral family"
      contains: "family: spectral"
    - path: "tests/unit/test_sensor_yamls_batch2.py"
      provides: "Validation tests for all 6 families"
  key_links:
    - from: "src/agentsim/knowledge_graph/sensors/*.yaml"
      to: "src/agentsim/knowledge_graph/models.py"
      via: "load_sensors() validates each sensor as SensorNode"
      pattern: "SensorNode"
---

<objective>
Create YAML definition files for the remaining 6 sensor families (3 LiDAR variants, Structured Light, Polarimetric, Spectral) following the template from Plan 01.

Purpose: Completes the sensor taxonomy data for LiDAR sub-families and remaining imaging modalities. Runs in parallel with Plan 02 (different files, same wave).

Output: 6 YAML files + validation test suite
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-sensor-taxonomy-population/07-01-SUMMARY.md
@src/agentsim/knowledge_graph/models.py
@src/agentsim/knowledge_graph/loader.py
@src/agentsim/knowledge_graph/sensors/spad.yaml
@.planning/research/FEATURES.md
@.planning/phases/07-sensor-taxonomy-population/07-RESEARCH.md

<interfaces>
<!-- FAMILY_SCHEMAS keys the executor must match for each family -->

From src/agentsim/knowledge_graph/models.py:
```python
SensorFamily.LIDAR_MECHANICAL: {
    "scan_rate_rpm": float,
    "angular_resolution_deg": float,
    "max_range_m": float,
}
SensorFamily.LIDAR_SOLID_STATE: {
    "flash_fov_deg": float,
    "point_density": float,
}
SensorFamily.LIDAR_FMCW: {
    "chirp_bandwidth_ghz": float,
    "chirp_duration_us": float,
    "coherence_length_m": float,
}
SensorFamily.STRUCTURED_LIGHT: {
    "pattern_type": str,
    "projector_resolution": str,
    "baseline_mm": float,
}
SensorFamily.POLARIMETRIC: {
    "extinction_ratio": float,
    "polarizer_angles_deg": str,
    "dolp_accuracy": float,
}
SensorFamily.SPECTRAL: {
    "spectral_range_nm_min": float,
    "spectral_range_nm_max": float,
    "spectral_resolution_nm": float,
    "band_count": (int, float),
}
```

YAML structure template (from spad.yaml):
```yaml
family: <SensorFamily.value>
display_name: "..."
description: "..."
ranges:
  <param_name>:
    min: <float>
    max: <float>
    typical: <float>
    unit: <pint-compatible>
    description: "..."
sensors:
  - name: "..."
    description: "..."
    source: "<datasheet or paper citation>"
    geometric: { fov: <float>, ... }
    temporal: { ... }
    radiometric: { ... }
    operational: { ... }
    family_specs: { <all FAMILY_SCHEMAS keys> }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create YAML files for LiDAR (3 variants), Structured Light, Polarimetric, and Spectral families</name>
  <files>
    src/agentsim/knowledge_graph/sensors/lidar_mechanical.yaml,
    src/agentsim/knowledge_graph/sensors/lidar_solid_state.yaml,
    src/agentsim/knowledge_graph/sensors/lidar_fmcw.yaml,
    src/agentsim/knowledge_graph/sensors/structured_light.yaml,
    src/agentsim/knowledge_graph/sensors/polarimetric.yaml,
    src/agentsim/knowledge_graph/sensors/spectral.yaml
  </files>
  <read_first>
    src/agentsim/knowledge_graph/models.py,
    src/agentsim/knowledge_graph/sensors/spad.yaml,
    src/agentsim/knowledge_graph/loader.py,
    .planning/research/FEATURES.md
  </read_first>
  <action>
    Create 6 YAML files following the exact template established by spad.yaml. Each file MUST:

    1. Have `family:` matching the SensorFamily enum value exactly
    2. Have `ranges:` section with min/max/typical for key parameters
    3. Have `sensors:` list with 2-3 concrete entries
    4. Each sensor entry MUST have `family_specs:` with ALL keys from FAMILY_SCHEMAS for that family
    5. Each sensor entry MUST have `source:` citing the datasheet or paper (per D-06)
    6. Use `null` for any value not confirmed from published source

    **Concrete sensor picks per family** (per D-09, from research):

    **lidar_mechanical.yaml** (family: lidar_mechanical):
    - Velodyne VLP-16 (budget, widely documented)
    - Ouster OS1-128 (mid-range, digital lidar)
    - Hesai Pandar128 (high-channel research)
    - family_specs keys: `scan_rate_rpm` (float), `angular_resolution_deg` (float), `max_range_m` (float)

    **lidar_solid_state.yaml** (family: lidar_solid_state):
    - Livox Mid-360 (budget, non-repetitive scan)
    - Innoviz InnovizTwo (automotive-grade)
    - Continental HRL131 (automotive flash)
    - family_specs keys: `flash_fov_deg` (float), `point_density` (float)

    **lidar_fmcw.yaml** (family: lidar_fmcw):
    - Aeva Aeries II (automotive FMCW)
    - SiLC Eyeonic (silicon photonics)
    - Bridger Photonics GAS-LDAR (methane detection, long-range)
    - family_specs keys: `chirp_bandwidth_ghz` (float), `chirp_duration_us` (float), `coherence_length_m` (float)

    **structured_light.yaml** (family: structured_light):
    - Intel RealSense D415 (consumer structured light depth)
    - Photoneo PhoXi 3D Scanner M (industrial)
    - Keyence LJ-X8000 (inline inspection)
    - family_specs keys: `pattern_type` (str), `projector_resolution` (str), `baseline_mm` (float)

    **polarimetric.yaml** (family: polarimetric):
    - Lucid Vision Phoenix PHX050S-P (machine vision, Sony IMX250MYR)
    - FLIR Blackfly S BFS-U3-51S5P-C (polarimetric variant)
    - 4D Technology PolarCam (research-grade snapshot)
    - family_specs keys: `extinction_ratio` (float), `polarizer_angles_deg` (str), `dolp_accuracy` (float)

    **spectral.yaml** (family: spectral):
    - Ximea MQ022HG-IM-SM5x5 (mosaic snapshot, 25 bands)
    - Specim FX17 (pushbroom NIR, 224 bands)
    - Headwall Nano-Hyperspec (airborne, 270 bands)
    - family_specs keys: `spectral_range_nm_min` (float), `spectral_range_nm_max` (float), `spectral_resolution_nm` (float), `band_count` (int or float)

    CRITICAL: All numeric values in `family_specs` that expect `float` in FAMILY_SCHEMAS must be written with a decimal point in YAML (e.g., `120.0` not `120`). For `(int, float)` fields like `band_count`, either form is acceptable.
  </action>
  <verify>
    <automated>cd /Users/default/mas-664/sim-agents && python -c "
from agentsim.knowledge_graph.loader import load_sensors
from agentsim.knowledge_graph.models import SensorFamily
families = [SensorFamily.LIDAR_MECHANICAL, SensorFamily.LIDAR_SOLID_STATE, SensorFamily.LIDAR_FMCW, SensorFamily.STRUCTURED_LIGHT, SensorFamily.POLARIMETRIC, SensorFamily.SPECTRAL]
for f in families:
    nodes = load_sensors(families=(f,))
    assert len(nodes) >= 2, f'{f.value}: expected >=2 sensors, got {len(nodes)}'
    print(f'{f.value}: {len(nodes)} sensors OK')
print('All 6 families validated')
"</automated>
  </verify>
  <acceptance_criteria>
    - src/agentsim/knowledge_graph/sensors/lidar_mechanical.yaml contains `family: lidar_mechanical`
    - src/agentsim/knowledge_graph/sensors/lidar_solid_state.yaml contains `family: lidar_solid_state`
    - src/agentsim/knowledge_graph/sensors/lidar_fmcw.yaml contains `family: lidar_fmcw`
    - src/agentsim/knowledge_graph/sensors/structured_light.yaml contains `family: structured_light`
    - src/agentsim/knowledge_graph/sensors/polarimetric.yaml contains `family: polarimetric`
    - src/agentsim/knowledge_graph/sensors/spectral.yaml contains `family: spectral`
    - Each YAML file contains at least 2 entries under `sensors:`
    - Each sensor entry contains a `source:` field
    - load_sensors() returns at least 2 SensorNode per family without validation errors
  </acceptance_criteria>
  <done>6 YAML files created for LiDAR variants, structured light, polarimetric, and spectral families. All load into validated SensorNode objects.</done>
</task>

<task type="auto">
  <name>Task 2: Create validation test suite for batch 2 sensor families</name>
  <files>
    tests/unit/test_sensor_yamls_batch2.py
  </files>
  <read_first>
    src/agentsim/knowledge_graph/loader.py,
    src/agentsim/knowledge_graph/models.py,
    tests/unit/test_kg_loader.py
  </read_first>
  <action>
    Create `tests/unit/test_sensor_yamls_batch2.py` with the same validation pattern as batch 1:

    For EACH of the 6 families (LIDAR_MECHANICAL, LIDAR_SOLID_STATE, LIDAR_FMCW, STRUCTURED_LIGHT, POLARIMETRIC, SPECTRAL):

    1. `test_{family}_loads_without_errors`: Call `load_sensors(families=(SensorFamily.{FAMILY},))`, assert returns tuple, assert `len >= 2`
    2. `test_{family}_all_have_source`: Load YAML directly, verify each sensor entry has `source` field
    3. `test_{family}_ranges_present`: Call `load_family_ranges()`, assert family in result and ranges dict non-empty
    4. `test_{family}_family_specs_complete`: For each SensorNode, verify all FAMILY_SCHEMAS keys present in `family_specs`

    Also add:
    - `test_all_batch2_sensors_count`: Load all sensors for batch 2 families, assert total >= 12 (6 families x 2 minimum)
    - `test_all_14_families_covered`: Load ALL sensors (no filter), group by family, assert all 14 SensorFamily enum values have at least 1 sensor. This is the comprehensive coverage check across Plans 01-03.

    Use `from __future__ import annotations`, `import pytest`, parametrize where appropriate.
  </action>
  <verify>
    <automated>cd /Users/default/mas-664/sim-agents && python -m pytest tests/unit/test_sensor_yamls_batch2.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_sensor_yamls_batch2.py contains `test_lidar_mechanical_loads_without_errors`
    - tests/unit/test_sensor_yamls_batch2.py contains `test_all_14_families_covered`
    - tests/unit/test_sensor_yamls_batch2.py contains `test_all_batch2_sensors_count`
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>All 6 batch 2 families validated. All 14 SensorFamily enum members have YAML coverage confirmed by test_all_14_families_covered.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/test_sensor_yamls_batch2.py -x -v` all pass
- `python -c "from agentsim.knowledge_graph import load_sensors; print(len(load_sensors()))"` prints >= 29 (3 SPAD + 14 batch1 + 12 batch2)
</verification>

<success_criteria>
- 6 YAML files created with complete family definitions per D-01, D-04, D-06, D-07
- All 14 SensorFamily enum members have at least one YAML file
- All concrete sensors have published datasheet sources per D-06
- All YAMLs load into validated SensorNode objects
</success_criteria>

<output>
After completion, create `.planning/phases/07-sensor-taxonomy-population/07-03-SUMMARY.md`
</output>
