---
phase: 07-sensor-taxonomy-population
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/agentsim/knowledge_graph/sensors/cw_tof.yaml
  - src/agentsim/knowledge_graph/sensors/pulsed_dtof.yaml
  - src/agentsim/knowledge_graph/sensors/event_camera.yaml
  - src/agentsim/knowledge_graph/sensors/coded_aperture.yaml
  - src/agentsim/knowledge_graph/sensors/light_field.yaml
  - src/agentsim/knowledge_graph/sensors/lensless.yaml
  - src/agentsim/knowledge_graph/sensors/rgb.yaml
  - tests/unit/test_sensor_yamls_batch1.py
autonomous: true
requirements: [SENS-02, SENS-03, SENS-04, SENS-05, SENS-07, SENS-08]

must_haves:
  truths:
    - "CW ToF YAML loads into validated SensorNode objects"
    - "Pulsed dToF YAML loads into validated SensorNode objects"
    - "Event camera YAML loads into validated SensorNode objects"
    - "Coded aperture YAML loads into validated SensorNode objects"
    - "Light field YAML loads into validated SensorNode objects"
    - "Lensless YAML loads into validated SensorNode objects"
    - "RGB YAML loads with Intel RealSense D435i per D-08"
    - "Each family YAML has family-level ranges and 2-3 concrete sensors"
  artifacts:
    - path: "src/agentsim/knowledge_graph/sensors/cw_tof.yaml"
      provides: "CW ToF family with 2-3 concrete sensors"
      contains: "family: cw_tof"
    - path: "src/agentsim/knowledge_graph/sensors/pulsed_dtof.yaml"
      provides: "Pulsed dToF family with 2-3 concrete sensors"
      contains: "family: pulsed_dtof"
    - path: "src/agentsim/knowledge_graph/sensors/event_camera.yaml"
      provides: "Event camera family with 2-3 concrete sensors"
      contains: "family: event_camera"
    - path: "src/agentsim/knowledge_graph/sensors/coded_aperture.yaml"
      provides: "Coded aperture family with 2-3 concrete sensors"
      contains: "family: coded_aperture"
    - path: "src/agentsim/knowledge_graph/sensors/light_field.yaml"
      provides: "Light field family with 2-3 concrete sensors"
      contains: "family: light_field"
    - path: "src/agentsim/knowledge_graph/sensors/lensless.yaml"
      provides: "Lensless camera family with 2-3 concrete sensors"
      contains: "family: lensless"
    - path: "src/agentsim/knowledge_graph/sensors/rgb.yaml"
      provides: "RGB camera family with RealSense D435i + 2 more"
      contains: "family: rgb"
    - path: "tests/unit/test_sensor_yamls_batch1.py"
      provides: "Validation tests for all 7 families"
  key_links:
    - from: "src/agentsim/knowledge_graph/sensors/*.yaml"
      to: "src/agentsim/knowledge_graph/models.py"
      via: "load_sensors() validates each sensor as SensorNode"
      pattern: "SensorNode"
---

<objective>
Create YAML definition files for 7 sensor families (CW ToF, Pulsed dToF, Event Camera, Coded Aperture, Light Field, Lensless, RGB) following the SPAD template from Plan 01.

Purpose: Populates the first batch of sensor families with complete physics specs and concrete sensor configurations from published datasheets (per D-06). These are consumed by Phase 9 graph seeding and Phase 8 CRB computation.

Output: 7 YAML files + validation test suite
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-sensor-taxonomy-population/07-01-SUMMARY.md
@src/agentsim/knowledge_graph/models.py
@src/agentsim/knowledge_graph/loader.py
@src/agentsim/knowledge_graph/sensors/spad.yaml
@.planning/research/FEATURES.md
@.planning/phases/07-sensor-taxonomy-population/07-RESEARCH.md

<interfaces>
<!-- FAMILY_SCHEMAS keys the executor must match for each family -->

From src/agentsim/knowledge_graph/models.py:
```python
SensorFamily.CW_TOF: {
    "modulation_frequency_mhz": float,
    "demodulation_contrast": float,
    "phase_tap_count": (int, float),
}
SensorFamily.PULSED_DTOF: {
    "pulse_width_ns": float,
    "laser_rep_rate_khz": float,
    "channel_count": (int, float),
}
SensorFamily.EVENT_CAMERA: {
    "contrast_threshold": float,
    "refractory_period_us": float,
    "bandwidth_khz": float,
}
SensorFamily.CODED_APERTURE: {
    "mask_pattern_type": str,
    "mask_transmittance": float,
    "psf_condition_number": float,
}
SensorFamily.LIGHT_FIELD: {
    "microlens_pitch_um": float,
    "angular_samples": (int, float),
    "baseline_mm": float,
}
SensorFamily.LENSLESS: {
    "mask_type": str,
    "mask_sensor_distance_mm": float,
    "psf_condition_number": float,
}
SensorFamily.RGB: {
    "pixel_pitch_um": float,
    "well_depth_electrons": float,
    "read_noise_electrons": float,
}
```

YAML structure template (from spad.yaml after Plan 01):
```yaml
family: <SensorFamily.value>
display_name: "..."
description: "..."
ranges:
  <param_name>:
    min: <float>
    max: <float>
    typical: <float>
    unit: <pint-compatible>
    description: "..."
sensors:
  - name: "..."
    description: "..."
    source: "<datasheet or paper citation>"
    geometric: { fov: <float>, ... }
    temporal: { ... }
    radiometric: { ... }
    operational: { ... }  # optional
    family_specs: { <all FAMILY_SCHEMAS keys for this family> }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create YAML files for CW ToF, Pulsed dToF, Event Camera, Coded Aperture, Light Field, Lensless, and RGB families</name>
  <files>
    src/agentsim/knowledge_graph/sensors/cw_tof.yaml,
    src/agentsim/knowledge_graph/sensors/pulsed_dtof.yaml,
    src/agentsim/knowledge_graph/sensors/event_camera.yaml,
    src/agentsim/knowledge_graph/sensors/coded_aperture.yaml,
    src/agentsim/knowledge_graph/sensors/light_field.yaml,
    src/agentsim/knowledge_graph/sensors/lensless.yaml,
    src/agentsim/knowledge_graph/sensors/rgb.yaml
  </files>
  <read_first>
    src/agentsim/knowledge_graph/models.py,
    src/agentsim/knowledge_graph/sensors/spad.yaml,
    src/agentsim/knowledge_graph/loader.py,
    .planning/research/FEATURES.md
  </read_first>
  <action>
    Create 7 YAML files following the exact template established by spad.yaml in Plan 01. Each file MUST:

    1. Have `family:` matching the SensorFamily enum value exactly
    2. Have `ranges:` section with min/max/typical for key parameters
    3. Have `sensors:` list with 2-3 concrete entries
    4. Each sensor entry MUST have `family_specs:` with ALL keys from FAMILY_SCHEMAS for that family
    5. Each sensor entry MUST have `source:` citing the datasheet or paper (per D-06)
    6. Use `null` for any value not confirmed from published source (per D-06)

    **Concrete sensor picks per family** (per D-09, from research):

    **cw_tof.yaml** (family: cw_tof):
    - PMD CamBoard pico flexx (PMD Technologies, budget)
    - Infineon REAL3 IRS2877C (mid-range)
    - Sony DepthSense IMX556 (research-grade)
    - family_specs keys: `modulation_frequency_mhz` (float), `demodulation_contrast` (float), `phase_tap_count` (int or float)

    **pulsed_dtof.yaml** (family: pulsed_dtof):
    - Garmin LIDAR-Lite v4 (budget)
    - Luminar Iris (mid-range automotive)
    - Leica RTC360 (survey-grade)
    - family_specs keys: `pulse_width_ns` (float), `laser_rep_rate_khz` (float), `channel_count` (int or float)

    **event_camera.yaml** (family: event_camera):
    - iniVation DVXplorer Lite (budget)
    - Prophesee EVK4 (mid-range, per SENS-03 requirement)
    - Samsung/Sony IMX636 (research-grade)
    - family_specs keys: `contrast_threshold` (float), `refractory_period_us` (float), `bandwidth_khz` (float)

    **coded_aperture.yaml** (family: coded_aperture):
    - Custom MURA mask + CMOS (academic baseline)
    - Coded aperture gamma camera (medical imaging)
    - Modified aperture research setup (academic characterised)
    - family_specs keys: `mask_pattern_type` (str), `mask_transmittance` (float), `psf_condition_number` (float)
    - Note: coded aperture sensors are largely research instruments. Use published experimental characterizations from papers as sources.

    **light_field.yaml** (family: light_field):
    - Lytro Illum (well-documented, discontinued)
    - Raytrix R42 (industrial)
    - Stanford camera array (academic multi-camera)
    - family_specs keys: `microlens_pitch_um` (float), `angular_samples` (int or float), `baseline_mm` (float)

    **lensless.yaml** (family: lensless):
    - DiffuserCam (UC Berkeley, academic)
    - PhlatCam (Rice University)
    - FlatScope (Rice University)
    - family_specs keys: `mask_type` (str), `mask_sensor_distance_mm` (float), `psf_condition_number` (float)
    - Note: lensless cameras are research systems. Use published paper characterizations as sources.

    **rgb.yaml** (family: rgb):
    - Intel RealSense D435i (per D-08, user-specified)
    - Sony IMX477 (Raspberry Pi HQ Camera)
    - FLIR Blackfly S BFS-U3-51S5C (machine vision)
    - family_specs keys: `pixel_pitch_um` (float), `well_depth_electrons` (float), `read_noise_electrons` (float)

    For EACH YAML file, include a `ranges:` section covering the key physics parameters for that family. Parameter ranges should reflect the published literature and datasheet ranges across commercial and research sensors. Use FEATURES.md ranges as a starting reference.

    CRITICAL: All numeric values in `family_specs` that expect `float` in FAMILY_SCHEMAS must be written with a decimal point in YAML (e.g., `10.0` not `10`). For `(int, float)` fields, either form is acceptable.
  </action>
  <verify>
    <automated>cd /Users/default/mas-664/sim-agents && python -c "
from agentsim.knowledge_graph.loader import load_sensors
from agentsim.knowledge_graph.models import SensorFamily
families = [SensorFamily.CW_TOF, SensorFamily.PULSED_DTOF, SensorFamily.EVENT_CAMERA, SensorFamily.CODED_APERTURE, SensorFamily.LIGHT_FIELD, SensorFamily.LENSLESS, SensorFamily.RGB]
for f in families:
    nodes = load_sensors(families=(f,))
    assert len(nodes) >= 2, f'{f.value}: expected >=2 sensors, got {len(nodes)}'
    print(f'{f.value}: {len(nodes)} sensors OK')
print('All 7 families validated')
"</automated>
  </verify>
  <acceptance_criteria>
    - src/agentsim/knowledge_graph/sensors/cw_tof.yaml contains `family: cw_tof`
    - src/agentsim/knowledge_graph/sensors/pulsed_dtof.yaml contains `family: pulsed_dtof`
    - src/agentsim/knowledge_graph/sensors/event_camera.yaml contains `family: event_camera`
    - src/agentsim/knowledge_graph/sensors/coded_aperture.yaml contains `family: coded_aperture`
    - src/agentsim/knowledge_graph/sensors/light_field.yaml contains `family: light_field`
    - src/agentsim/knowledge_graph/sensors/lensless.yaml contains `family: lensless`
    - src/agentsim/knowledge_graph/sensors/rgb.yaml contains `family: rgb`
    - src/agentsim/knowledge_graph/sensors/rgb.yaml contains `RealSense D435i`
    - Each YAML file contains at least 2 entries under `sensors:`
    - Each sensor entry contains a `source:` field (per D-06)
    - load_sensors() returns at least 2 SensorNode per family without validation errors
  </acceptance_criteria>
  <done>7 YAML files created, each loading into validated SensorNode objects. At least 2 concrete sensors per family with published datasheet values.</done>
</task>

<task type="auto">
  <name>Task 2: Create validation test suite for batch 1 sensor families</name>
  <files>
    tests/unit/test_sensor_yamls_batch1.py
  </files>
  <read_first>
    src/agentsim/knowledge_graph/loader.py,
    src/agentsim/knowledge_graph/models.py,
    tests/unit/test_kg_loader.py
  </read_first>
  <action>
    Create `tests/unit/test_sensor_yamls_batch1.py` with comprehensive validation tests:

    For EACH of the 7 families (CW_TOF, PULSED_DTOF, EVENT_CAMERA, CODED_APERTURE, LIGHT_FIELD, LENSLESS, RGB):

    1. `test_{family}_loads_without_errors`: Call `load_sensors(families=(SensorFamily.{FAMILY},))`, assert returns tuple, assert `len >= 2`
    2. `test_{family}_all_have_source`: For each loaded SensorNode, verify that the raw YAML data includes a `source` field (load YAML directly with yaml.safe_load to check this, since SensorNode doesn't have a `source` field)
    3. `test_{family}_ranges_present`: Call `load_family_ranges()`, assert family is in result dict, assert ranges dict is not empty
    4. `test_{family}_family_specs_complete`: For each SensorNode, verify all FAMILY_SCHEMAS keys are present in `family_specs`

    Also add:
    - `test_rgb_has_realsense_d435i`: Assert one SensorNode from RGB family has "RealSense D435i" in name (per D-08)
    - `test_all_batch1_sensors_count`: Load all sensors, assert total >= 14 (7 families x 2 minimum)

    Use `from __future__ import annotations`, `import pytest`, parametrize where appropriate.
  </action>
  <verify>
    <automated>cd /Users/default/mas-664/sim-agents && python -m pytest tests/unit/test_sensor_yamls_batch1.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_sensor_yamls_batch1.py contains `test_cw_tof_loads_without_errors`
    - tests/unit/test_sensor_yamls_batch1.py contains `test_rgb_has_realsense_d435i`
    - tests/unit/test_sensor_yamls_batch1.py contains `test_all_batch1_sensors_count`
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>All 7 families validated: YAML files load without errors, ranges present, family_specs complete, RealSense D435i confirmed in RGB family.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/test_sensor_yamls_batch1.py tests/unit/test_kg_loader.py -x -v` all pass
- `python -c "from agentsim.knowledge_graph import load_sensors; print(len(load_sensors()))"` prints >= 17 (3 SPAD + 14+ batch 1)
</verification>

<success_criteria>
- 7 YAML files created with complete family definitions per D-01, D-04, D-06, D-07
- RealSense D435i in RGB family per D-08
- All concrete sensors have published datasheet sources per D-06
- All YAMLs load into validated SensorNode objects
</success_criteria>

<output>
After completion, create `.planning/phases/07-sensor-taxonomy-population/07-02-SUMMARY.md`
</output>
