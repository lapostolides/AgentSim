---
phase: 02-computational-imaging-domain-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/agentsim/physics/checks/nlos_geometry.py
  - src/agentsim/physics/checker.py
  - src/agentsim/physics/__init__.py
  - src/agentsim/preview/scene_description.py
  - tests/unit/test_nlos_geometry.py
autonomous: true
requirements: [NLOS-01, NLOS-02, NLOS-03]

must_haves:
  truths:
    - "Three-bounce geometry validator confirms sensor sees relay wall, relay wall illuminates hidden scene, and return path is unoccluded"
    - "Sensor FOV checker validates SPAD field of view covers relay wall scan area"
    - "Temporal resolution validator checks time-bin width resolves hidden scene geometry"
    - "All NLOS checks return tuple[CheckResult, ...] following the existing check module pattern"
    - "Checker pipeline dispatches NLOS checks when domain is detected as nlos_transient_imaging"
  artifacts:
    - path: "src/agentsim/physics/checks/nlos_geometry.py"
      provides: "Three NLOS validation functions"
      exports: ["check_three_bounce_geometry", "check_sensor_fov", "check_temporal_resolution"]
    - path: "src/agentsim/physics/checker.py"
      provides: "Extended pipeline with NLOS domain dispatch"
      exports: ["run_deterministic_checks", "run_nlos_checks"]
    - path: "tests/unit/test_nlos_geometry.py"
      provides: "Tests for all three NLOS checks"
  key_links:
    - from: "src/agentsim/physics/checks/nlos_geometry.py"
      to: "src/agentsim/physics/models.py"
      via: "import CheckResult, Severity"
      pattern: "from agentsim\\.physics\\.models import CheckResult, Severity"
    - from: "src/agentsim/physics/checker.py"
      to: "src/agentsim/physics/checks/nlos_geometry.py"
      via: "import NLOS check functions"
      pattern: "from agentsim\\.physics\\.checks\\.nlos_geometry import"
    - from: "src/agentsim/physics/checker.py"
      to: "src/agentsim/physics/domains"
      via: "import detect_domain for dispatch"
      pattern: "from agentsim\\.physics\\.domains import detect_domain"
---

<objective>
Implement the three deterministic NLOS geometry validators (three-bounce path, sensor FOV, temporal resolution) and wire them into the checker pipeline with domain-based dispatch.

Purpose: These checks catch physically impossible NLOS scene configurations before execution -- the core differentiator from generic simulation tools.
Output: NLOS geometry check module, extended checker pipeline, Sensor model FOV field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-computational-imaging-domain-intelligence/02-RESEARCH.md
@.planning/phases/02-computational-imaging-domain-intelligence/02-01-SUMMARY.md
@src/agentsim/physics/models.py
@src/agentsim/physics/checker.py
@src/agentsim/physics/checks/mesh_quality.py
@src/agentsim/preview/scene_description.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NLOS geometry check functions and Sensor FOV field</name>
  <files>
    src/agentsim/physics/checks/nlos_geometry.py,
    src/agentsim/preview/scene_description.py,
    tests/unit/test_nlos_geometry.py
  </files>
  <read_first>
    src/agentsim/physics/checks/mesh_quality.py (existing check pattern: pure functions returning tuple[CheckResult, ...]),
    src/agentsim/physics/models.py (CheckResult and Severity imports),
    src/agentsim/preview/scene_description.py (Vec3, Sensor, RelayWall, Occluder models),
    .planning/phases/02-computational-imaging-domain-intelligence/02-RESEARCH.md (geometry math examples, pitfalls 1-5)
  </read_first>
  <behavior>
    - Test 1: check_three_bounce_geometry with valid confocal setup (sensor at (0,-1.5,0), wall at origin, hidden at (0,1,0)) returns no ERRORs
    - Test 2: check_three_bounce_geometry with sensor behind wall (sensor at (0,1,0), wall normal toward -y) returns ERROR "sensor cannot see relay wall"
    - Test 3: check_three_bounce_geometry with wall normal pointing away from sensor returns ERROR about wall normal
    - Test 4: check_three_bounce_geometry with occluder blocking sensor-to-wall path returns ERROR about occlusion
    - Test 5: check_three_bounce_geometry with no occluder (open NLOS) returns no ERROR for occluder check (optional)
    - Test 6: check_sensor_fov with sensor FOV=20deg covering 2m wall at 1.5m distance returns INFO (sufficient)
    - Test 7: check_sensor_fov with sensor FOV=1deg (too narrow) returns ERROR about insufficient FOV
    - Test 8: check_temporal_resolution with dt=32ps, min_feature_sep=0.05m returns INFO (sufficient, c*32ps/2 = 4.8mm < 50mm)
    - Test 9: check_temporal_resolution with dt=1000ps, min_feature_sep=0.01m returns ERROR (c*1000ps/2 = 150mm > 10mm)
    - Test 10: check_temporal_resolution uses round-trip formula (c*dt/2), not one-way
    - Test 11: Sensor model has fov_degrees field with default 20.0
    - Test 12: check_three_bounce_geometry with non-confocal setup (separate laser_pos and sensor_pos) returns no ERROR for valid geometry
  </behavior>
  <action>
    1. Add `fov_degrees: float = 20.0` field to the `Sensor` model in `src/agentsim/preview/scene_description.py`. Add it after the `laser_target` field with description="Sensor field of view in degrees". The Sensor model is frozen so this is just adding a field with a default.

    2. Create `src/agentsim/physics/checks/nlos_geometry.py`:
       ```python
       """NLOS transient imaging geometry validation.

       Deterministic checks for three-bounce light path feasibility,
       sensor FOV coverage, and temporal resolution sufficiency.
       No LLM calls, no side effects, pure geometry math.
       """
       from __future__ import annotations

       import math
       import numpy as np
       from agentsim.physics.models import CheckResult, Severity
       ```

       Implement three public functions:

       **check_three_bounce_geometry(
           sensor_pos: tuple[float, float, float],
           sensor_look_at: tuple[float, float, float],
           relay_wall_pos: tuple[float, float, float],
           relay_wall_normal: tuple[float, float, float],
           relay_wall_size: float,
           hidden_objects: tuple[tuple[float, float, float], ...],
           occluder_pos: tuple[float, float, float] | None = None,
           occluder_size: tuple[float, float, float] | None = None,
           sensor_fov_deg: float = 20.0,
       ) -> tuple[CheckResult, ...]:**

       Checks:
       a) Sensor can see relay wall: compute direction from sensor to wall center, check dot product with sensor look-at direction is within FOV cone (cos(angle) >= cos(fov/2)), AND wall normal dot (-direction) > 0 (wall faces sensor).
       b) Relay wall normal faces sensor hemisphere: np.dot(wall_normal, -(wall_pos - sensor_pos)/norm) > 0. If negative, ERROR.
       c) Hidden objects behind occluder (only if occluder is provided): for each hidden object, check it is on the far side of the occluder from the sensor. Use occluder position as a dividing plane. If hidden object is on sensor side, WARNING.
       d) Return path check: relay wall can "see" hidden objects (simplified: hidden objects are in the hemisphere that the wall normal's back side faces, i.e., on the opposite side of the wall from the sensor).

       Use numpy for vector math. Guard against zero-norm vectors (return ERROR). All results have check="nlos_three_bounce".

       **check_sensor_fov(
           sensor_pos: tuple[float, float, float],
           sensor_look_at: tuple[float, float, float],
           sensor_fov_deg: float,
           relay_wall_pos: tuple[float, float, float],
           relay_wall_size: float,
       ) -> tuple[CheckResult, ...]:**

       Compute the angular extent of the relay wall as seen from the sensor. Compare with sensor FOV. If wall angular extent > FOV, ERROR with message about insufficient FOV. Use `2 * math.atan2(wall_size/2, distance)` for wall angular extent (half-angle). check="nlos_sensor_fov".

       **check_temporal_resolution(
           time_bin_ps: float,
           min_feature_separation_m: float,
           wall_to_object_distance_m: float | None = None,
       ) -> tuple[CheckResult, ...]:**

       Compute spatial resolution = SPEED_OF_LIGHT * (time_bin_ps * 1e-12) / 2.0 (round-trip, per Pitfall 2). If spatial_resolution >= min_feature_separation_m, ERROR. Use SPEED_OF_LIGHT = 299_792_458.0 from constant (import from agentsim.physics.constants). check="nlos_temporal_resolution".

    3. Create `tests/unit/test_nlos_geometry.py` with the 12 tests from <behavior>.

    CRITICAL PITFALLS TO AVOID:
    - Pitfall 2: ALWAYS use round-trip distance (c*dt/2) for temporal resolution, NOT one-way.
    - Pitfall 4: Occluder is OPTIONAL. If occluder_pos is None, skip occluder checks entirely.
    - Pitfall 5: Relay wall normal convention: normal points TOWARD SENSOR (outward normal). The default is Vec3(x=0, y=-1, z=0).
  </action>
  <verify>
    <automated>cd /Users/default/mas-664/sim-agents && python -m pytest tests/unit/test_nlos_geometry.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def check_three_bounce_geometry" src/agentsim/physics/checks/nlos_geometry.py returns 0
    - grep -q "def check_sensor_fov" src/agentsim/physics/checks/nlos_geometry.py returns 0
    - grep -q "def check_temporal_resolution" src/agentsim/physics/checks/nlos_geometry.py returns 0
    - grep -q "fov_degrees" src/agentsim/preview/scene_description.py returns 0
    - grep -q "299_792_458" src/agentsim/physics/checks/nlos_geometry.py returns 0 (speed of light used)
    - grep -q "/ 2" src/agentsim/physics/checks/nlos_geometry.py returns 0 (round-trip formula)
    - python -m pytest tests/unit/test_nlos_geometry.py -x passes all 12 tests
  </acceptance_criteria>
  <done>
    Three NLOS geometry check functions implemented with proper round-trip temporal resolution, optional occluder handling, and correct wall normal convention. Sensor model has fov_degrees field. All 12 tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire NLOS checks into checker pipeline with domain dispatch</name>
  <files>
    src/agentsim/physics/checker.py,
    src/agentsim/physics/__init__.py,
    tests/unit/test_checker_pipeline.py
  </files>
  <read_first>
    src/agentsim/physics/checker.py (current 6-check pipeline with _has_error and _make_report),
    src/agentsim/physics/__init__.py (current exports),
    src/agentsim/physics/checks/nlos_geometry.py (check functions created in Task 1),
    src/agentsim/physics/domains/__init__.py (detect_domain, load_domain from Plan 01),
    tests/unit/test_checker_pipeline.py (existing pipeline tests to not break)
  </read_first>
  <behavior>
    - Test 1: run_nlos_checks with valid confocal geometry returns passed=True
    - Test 2: run_nlos_checks with invalid geometry (sensor behind wall) returns passed=False
    - Test 3: run_nlos_checks with insufficient temporal resolution returns ERROR result
    - Test 4: run_deterministic_checks with domain="nlos_transient_imaging" and nlos_scene_params includes NLOS checks in results
    - Test 5: run_deterministic_checks with domain="universal" (default) does NOT run NLOS checks
    - Test 6: Existing pipeline tests still pass (no regressions)
  </behavior>
  <action>
    1. Add `run_nlos_checks` function to `src/agentsim/physics/checker.py`:
       ```python
       def run_nlos_checks(
           sensor_pos: tuple[float, float, float],
           sensor_look_at: tuple[float, float, float],
           relay_wall_pos: tuple[float, float, float],
           relay_wall_normal: tuple[float, float, float],
           relay_wall_size: float,
           hidden_objects: tuple[tuple[float, float, float], ...],
           sensor_fov_deg: float = 20.0,
           time_bin_ps: float | None = None,
           min_feature_separation_m: float | None = None,
           occluder_pos: tuple[float, float, float] | None = None,
           occluder_size: tuple[float, float, float] | None = None,
       ) -> ValidationReport:
       ```
       This function:
       - Calls check_three_bounce_geometry with all geometry params
       - Calls check_sensor_fov with sensor params and wall params
       - If time_bin_ps and min_feature_separation_m are provided, calls check_temporal_resolution
       - Aggregates all CheckResults into a ValidationReport
       - Uses _has_error and _make_report helpers

    2. Extend `run_deterministic_checks` to accept optional `nlos_scene_params: dict | None = None` parameter. After step 6 (mesh quality), if domain == "nlos_transient_imaging" or nlos_scene_params is provided, extract NLOS geometry from nlos_scene_params dict and call run_nlos_checks. Expected dict keys:
       - sensor_pos, sensor_look_at, relay_wall_pos, relay_wall_normal, relay_wall_size
       - hidden_objects, sensor_fov_deg, time_bin_ps, min_feature_separation_m
       - occluder_pos (optional), occluder_size (optional)

       Add imports at top of checker.py:
       ```python
       from agentsim.physics.checks.nlos_geometry import (
           check_three_bounce_geometry,
           check_sensor_fov,
           check_temporal_resolution,
       )
       ```

    3. Update `src/agentsim/physics/__init__.py` to export run_nlos_checks:
       ```python
       from agentsim.physics.checker import run_deterministic_checks, run_nlos_checks
       __all__ = ["run_deterministic_checks", "run_nlos_checks"]
       ```

    4. Add 6 new tests to `tests/unit/test_checker_pipeline.py` (append, do not overwrite existing tests).
  </action>
  <verify>
    <automated>cd /Users/default/mas-664/sim-agents && python -m pytest tests/unit/test_checker_pipeline.py tests/unit/test_nlos_geometry.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def run_nlos_checks" src/agentsim/physics/checker.py returns 0
    - grep -q "nlos_scene_params" src/agentsim/physics/checker.py returns 0
    - grep -q "run_nlos_checks" src/agentsim/physics/__init__.py returns 0
    - grep -q "from agentsim.physics.checks.nlos_geometry import" src/agentsim/physics/checker.py returns 0
    - python -m pytest tests/unit/test_checker_pipeline.py -x passes (existing + new tests)
    - python -m pytest tests/unit/test_nlos_geometry.py -x passes
  </acceptance_criteria>
  <done>
    NLOS checks wired into checker pipeline. run_nlos_checks available as standalone function. run_deterministic_checks dispatches NLOS checks when domain is nlos_transient_imaging or nlos_scene_params provided. All existing pipeline tests still pass.
  </done>
</task>

</tasks>

<verification>
cd /Users/default/mas-664/sim-agents && python -m pytest tests/unit/test_nlos_geometry.py tests/unit/test_checker_pipeline.py -x -v
cd /Users/default/mas-664/sim-agents && python -c "from agentsim.physics import run_nlos_checks; r = run_nlos_checks(sensor_pos=(0,-1.5,0), sensor_look_at=(0,0,0), relay_wall_pos=(0,0,0), relay_wall_normal=(0,-1,0), relay_wall_size=2.0, hidden_objects=((0,1,0),)); print('passed:', r.passed)"
</verification>

<success_criteria>
- Three NLOS check functions return CheckResults with correct severity for valid/invalid geometries
- Sensor model has fov_degrees field
- Checker pipeline dispatches NLOS checks for nlos_transient_imaging domain
- All existing tests pass without regression
- Temporal resolution uses round-trip formula (c*dt/2)
</success_criteria>

<output>
After completion, create `.planning/phases/02-computational-imaging-domain-intelligence/02-02-SUMMARY.md`
</output>
